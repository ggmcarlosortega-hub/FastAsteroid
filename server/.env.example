PORT=4000

DB_HOST=localhost
DB_PORT=3306
DB_USER=fasteroid_app
DB_PASSWORD=
DB_NAME=fasteroid

# Genera uno con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
AUTH_SECRET=
